// Custom React hooks (server-state via TanStack Query, UI state via Zustand).
// Components call these hooks instead of holding business logic (Rule #4).
// Re-export hooks as they are added.
export {};
